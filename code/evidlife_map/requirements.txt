# Minimal P1 dependency set. Pinned only to majors; concrete versions live in the
# Docker image. Anything GPU-only is flagged inline so the maintainer can swap to
# CPU wheels when bringing up the CI container.
#
# GPU-only (will fail to install without CUDA 12 toolchain on the host or in the
# build image):
#   * torch  (we use the official cu121 wheel inside the Docker image)
#
# CPU wheels work fine for everything else.

numpy>=1.26,<2.1
scipy>=1.11
torch>=2.2,<2.6           # GPU-only on host; CI uses cpu wheel
torchvision>=0.17         # for the Cylinder3D fine-tune pipeline; ok on cpu
open3d>=0.18              # voxel visualisation + ICP for M2 (Eq 9)
nuscenes-devkit>=1.1.11   # data/nuscenes_lidarseg.py
pypcd4>=1.0               # KITTI-360 .pcd / .bin loaders
pyyaml>=6.0
tqdm>=4.66
typing_extensions>=4.10
scikit-learn>=1.4         # AUROC / AUPR / ECE bin assignment
pytest>=8.0
pytest-cov>=5.0
hypothesis>=6.100         # property tests for the α ≥ 1 invariant
